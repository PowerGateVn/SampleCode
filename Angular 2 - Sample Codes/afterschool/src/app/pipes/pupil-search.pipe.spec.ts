import { PupilSearchPipe } from './pupil-search.pipe';

describe('PupilSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PupilSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
