import { StatusSortPipe } from './status-sort.pipe';

describe('StatusSortPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusSortPipe();
    expect(pipe).toBeTruthy();
  });
});
